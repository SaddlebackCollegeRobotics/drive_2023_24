# Resources
# https://docs.odriverobotics.com/v/latest/fibre_types/com_odriverobotics_ODrive.html
# https://docs.odriverobotics.com/v/latest/guides/can-guide.html
# https://www.csselectronics.com/pages/dbc-editor-can-bus-database
# https://docs.odriverobotics.com/releases/firmware
# https://docs.odriverobotics.com/v/latest/manual/can-protocol.html



# List of necessary function calls, read and writes, for the initial CAN bus script.

- Set axis state: IDLE, CLOSED_LOOP_CONTROL, FULL_CALIBRATION_SEQUENCE

- Check if motor is calibrated: 
    - config.motor.phase_resistance_valid bool
    - config.motor.phase_inductance_valid bool

- Save configuration
- Erase configuration (Note: Erasing configuration will remove can bus config as well)

- Clear_errors()
- Get_errors()
    - Gives disarm reason***


- Read controller heartbeat
    - This gives procefure result, axis state, and axis, error.

- Set control mode (velocity, pos, etc)
- Set input mode (Inactive, passthrough, vel_ramp)

- input_pos for pos control
- input_vel for vel control

- Assert version (endpoint lookup and make sure all fw is the required one across controllers.)

- Identify

- max_regen_current

check this value is correct:
- dc_max_negative_current

- watchdog_feed()

- enable_dc_bus_voltage_feedback (check to make sure this value is correct)
    - Needs ramp start and end config setting.

- axis.config.motor.direction is +1 or -1 is only for calibration???

- disable uneccesary cyclical calls from motor controllers.

- disable enable_overspeed_error??

- Be able to set pos_gain, vel_gain, vel_integrator_gain, etc.

- disable vel limit (infinity to disable) and vel tolerance

- Will be necessary be able to change a config value across multiple odrives simultaneously.